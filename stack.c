#include "monty.h"
/**
 * f_stack - function also prints the top
 * @head: head stack
 * @counter: line number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
